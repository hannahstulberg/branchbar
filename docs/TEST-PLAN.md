# TEST PLAN — BranchBar

Named invariants from PLAN.md §7, mapped to test files as packets land. Fixture inventory: `recorded-*` files come from `make record-fixtures` (real `/usr/bin/git` 2.39.5 and `gh`); `synthetic-*` files are hand-extended cases and say so in a header comment.

| Invariant | Test file | Packet | Fixture |
|---|---|---|---|
| (filled as packets land) | | | |
