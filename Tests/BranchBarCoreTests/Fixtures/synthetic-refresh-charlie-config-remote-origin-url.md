# synthetic: `config --get remote.origin.url` for the third repo

An HTTPS github.com remote with the `.git` suffix, in the shape the recorded pair records. It
resolves to the slug `github.com/hannahstulberg/charlie`, distinct from the two recorded slugs, so
the lazy-PR tests can name which repo a `gh pr list --repo …` call belongs to.
