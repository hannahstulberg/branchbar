# synthetic: a reflog file that exists and is zero bytes

Hand-written twin of `recorded-reflog-hannah-personal-agent-origin-updates-3-29-26.txt`, which really is 0 bytes on this machine and is the observation that made "reflog files can exist and be empty" a rule in CLAUDE.md. Kept as a synthetic as well so the case survives a re-record on a different machine.

Invariant: `emptyReflogFileFallsBackToTipCommitDate`.
