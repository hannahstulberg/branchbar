# synthetic: `worktree list --porcelain -z` for the third repo

One primary worktree on `refs/heads/main`, in the NUL-delimited shape the recorded pair records
(`worktree`, `HEAD`, `branch`, each followed by `\0`, then one more `\0` closing the record). The paths and the object name match
`synthetic-refresh-charlie-rev-parse.txt` and `synthetic-refresh-charlie-for-each-ref-heads.txt`.
