# synthetic: `worktree list --porcelain` for the third repo

One primary worktree on `refs/heads/main`, in the four-line shape the recorded pair records
(`worktree`, `HEAD`, `branch`, blank terminator). The paths and the object name match
`synthetic-refresh-charlie-rev-parse.txt` and `synthetic-refresh-charlie-for-each-ref-heads.txt`.
